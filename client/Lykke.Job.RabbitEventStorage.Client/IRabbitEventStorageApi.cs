using JetBrains.Annotations;
using Refit;

namespace Lykke.Job.RabbitEventStorage.Client
{
    // This is an example of service controller interfaces.
    // Actual interface methods must be placed here (not in IRabbitEventStorageClient interface).

    /// <summary>
    /// RabbitEventStorage client API interface.
    /// </summary>
    [PublicAPI]
    public interface IRabbitEventStorageApi
    {
    }
}
